<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yintu.rixing.archives.ArchQuantityStatisticsMapper">


    <select id="findArchivesData" resultType="java.util.Map">
        SELECT count(*) as `count`, `status`
        FROM ${tableName}
        <where>
            <if test="startDate!=null and endDate!=null">
                DATE(`create_time`) BETWEEN #{startDate,jdbcType=TIMESTAMP} AND #{endDate,jdbcType=TIMESTAMP}
            </if>
        </where>
        GROUP BY `status`
    </select>

</mapper>
