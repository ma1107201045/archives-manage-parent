jwt:
  # 加密密钥
  secret: jellymilk
  # token有效时长  1小时：3600  1天:86400
  expire: 3600
  # header 名称
  header: Authorization
  #发行人
  issuer: system
  #忽略token验证的 请求
  ignores:
    - /remote/remo-auth/login
    - /remote/remo-auth/register